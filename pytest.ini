[pytest]
addopts = --basetemp=tests/.pytest-tmp-current
cache_dir = tests/.pytest-cache
markers =
    gpu: requires a local CUDA runtime and CuPy
    live_runpod: submits an explicitly authorized billable RunPod request
